module DayXXSpec where

import DayXX
import Test.Hspec

spec :: Spec
spec = do
  it "part A" $ do
    True `shouldBe` True
    False `shouldBe` False
  it "part B" $ do
    True `shouldBe` True
    False `shouldBe` False
